@model ExpenseTracker.Models.Categoria
@{
    ViewData["Title"] = "Eliminar Categoría";
}

<h2 class="mb-4 text-danger">¿Estás seguro que deseas eliminar esta categoría?</h2>

<div class="alert alert-warning">
    Esta acción solo eliminará la categoría visualmente. Los datos se mantendrán para reportes.
</div>

<div class="mb-3">
    <dl class="row">
        <dt class="col-sm-2">Título</dt>
        <dd class="col-sm-10">@Model.Titulo</dd>

        <dt class="col-sm-2">Descripción</dt>
        <dd class="col-sm-10">@Model.Descripcion</dd>

        <dt class="col-sm-2">% Máximo</dt>
        <dd class="col-sm-10">@Model.PorcentajeMaximo</dd>
    </dl>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Confirmar Eliminación</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>
